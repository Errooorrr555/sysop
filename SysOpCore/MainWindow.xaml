<Window x:Class="SysOpCore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:SysOpCore.Models"
        mc:Ignorable="d"
        Title="SysOpCore" Height="820" Width="1400" WindowStartupLocation="CenterScreen"
        Background="{StaticResource WindowBg}" Foreground="White">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource PanelBg}" Padding="16">
            <StackPanel>
                <TextBlock Text="{Binding Header}" FontSize="14" FontWeight="SemiBold"/>
                <Border x:Name="WinReBanner" Margin="0,8,0,0" Background="#553A2E" Padding="8" CornerRadius="6" Visibility="Collapsed">
                    <TextBlock Text="WinRE session detected: high-risk actions are flagged and animations should stay minimal."/>
                </Border>
            </StackPanel>
        </Border>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="96"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Background="{StaticResource PanelBg}" Grid.Column="0">
                <DockPanel>
                    <Button DockPanel.Dock="Bottom" Margin="10" Padding="8" Click="Settings_Click" Content="⚙" ToolTip="Settings"/>
                    <StackPanel>
                        <TextBlock Text="SOC" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,12"/>
                        <ListBox ItemsSource="{Binding Navigation}" SelectedItem="{Binding SelectedNav}"
                                 Background="Transparent" BorderThickness="0" Foreground="White" HorizontalContentAlignment="Center">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <Border Margin="8" Padding="8" Background="#2A3240" CornerRadius="10">
                                        <TextBlock Text="{Binding}" TextAlignment="Center" FontSize="10" TextWrapping="Wrap"/>
                                    </Border>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </StackPanel>
                </DockPanel>
            </Border>

            <Grid Grid.Column="1" Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="180"/>
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0">
                    <ItemsControl ItemsSource="{Binding Cards}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate DataType="{x:Type models:ActionCard}">
                                <Border Width="310" Height="180" Background="{StaticResource CardBg}" Margin="8" Padding="14" CornerRadius="12">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <TextBlock Text="{Binding Title}" FontSize="15" FontWeight="Bold"/>
                                        <TextBlock Grid.Row="1" Text="{Binding Description}" Opacity="0.9" TextWrapping="Wrap" Margin="0,8,0,0"/>
                                        <Button Grid.Row="2" Content="Run" Tag="{Binding}" Click="RunCard_Click"
                                                HorizontalAlignment="Left" Padding="12,4" Margin="0,8,0,0"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <Border Grid.Row="1" Background="#0E1117" CornerRadius="8" Padding="10" Margin="8,10,8,0">
                    <ScrollViewer>
                        <TextBlock Text="{Binding Output}" FontFamily="Consolas" TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Border>
            </Grid>

            <Border x:Name="SettingsPane" Grid.Column="1" HorizontalAlignment="Right" Width="340" Margin="0,16,16,16"
                    Background="#20242C" Padding="14" CornerRadius="12" Visibility="Collapsed">
                <StackPanel>
                    <TextBlock Text="Settings" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>
                    <CheckBox Content="Always on Top" IsChecked="{Binding AlwaysOnTop}" Checked="AlwaysOnTop_Changed" Unchecked="AlwaysOnTop_Changed"/>
                    <CheckBox Content="Use monospace font in logs" IsChecked="{Binding UseMonospaceFont}" Margin="0,6,0,0"/>
                    <TextBlock Text="WinRE Save Path" Margin="0,12,0,4"/>
                    <TextBox x:Name="SavePathBox" Text="{Binding WinReSavePath}"/>
                    <Button Content="Browse" Margin="0,6,0,0" Click="BrowseSavePath_Click"/>
                    <TextBlock Margin="0,12,0,0" Text="{Binding Header}" TextWrapping="Wrap"/>
                    <Button Content="Disable Convenient Launch" Margin="0,12,0,0" Click="DisableLaunch_Click"/>
                    <Button Content="Close" Margin="0,8,0,0" Click="Settings_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </DockPanel>
</Window>
